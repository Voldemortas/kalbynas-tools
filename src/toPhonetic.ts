import { notImplemented } from './utils'

export type toPhoneticOptions = {}

export default function toPhonetic(
  wordOrSentence: string,
  options: toPhoneticOptions
): string {
  throw notImplemented
}
