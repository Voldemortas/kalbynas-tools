import { notImplemented } from './utils'

export type syllabiseOptions = {}

export default function syllabise(
  wordOrSentence: string,
  options: syllabiseOptions
): string {
  throw notImplemented
}
